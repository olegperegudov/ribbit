<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Signing with a real identity (the stable "Ribbit Code Signing" cert, 0.7.67+)
  makes the Tauri bundler apply the hardened runtime — ad-hoc builds weren't
  hardened. A hardened process may only use the microphone if it carries the
  audio-input entitlement; without it macOS denies instantly and silently: no
  consent prompt, no row in Privacy → Microphone, and CoreAudio hands cpal a
  stream of zeros (the RMS=0 "mic is silent" failure). This file rides along on
  every codesign via bundle.macOS.entitlements in tauri.conf.json.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
